python3>=3.10

# Optional but recommended fallback tools for PDF rendering/metadata when
# PyMuPDF is not installed in the active Python environment.
poppler-utils

# Optional but preferred terminal thumbnail renderer. Kannon uses this
# automatically when available unless --no-chafa is supplied.
chafa
